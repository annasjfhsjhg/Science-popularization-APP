<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kepuxingqiu.mapper.UserEncyclopediaMapper">

    <select id="countUnlocked" resultType="int">
        SELECT COUNT(*) FROM user_encyclopedia WHERE user_id = #{userId}
    </select>

    <select id="countUnlockedByCategory" resultType="int">
        SELECT COUNT(*)
        FROM user_encyclopedia ue
        INNER JOIN encyclopedia e ON ue.encyclopedia_id = e.id
        WHERE ue.user_id = #{userId}
          AND e.category = #{category}
    </select>

</mapper>
